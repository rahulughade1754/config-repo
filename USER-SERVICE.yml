spring:
  application:
    name: USER-SERVICE

  config:
    # Import from Config Server (optional, won't fail if server is down)
    import: optional:configserver:http://localhost:8888

  datasource:
    url: jdbc:mysql://localhost:3306/hotelbooking_user?useSSL=false&serverTimezone=UTC
    username: root
    password: Anandrao@6261
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# JWT settings
jwt:
  secret: mysecretkey123456
  expiration: 3600000 # 1 hour

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG

